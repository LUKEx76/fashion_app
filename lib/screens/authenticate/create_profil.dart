import 'package:flutter/material.dart';

class CreateProfil extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Create Profil Screen"),
    );
  }
}
